/**
 * Точка входа для serverless-хостинга.
 *
 * Express-приложение само по себе является обработчиком (req, res), поэтому
 * отдаём его как есть. Логика живёт в createApp — один и тот же код обслуживает
 * и долгоживущий процесс (npm start), и serverless.
 *
 * Отличие среды: каждый холодный экземпляр начинает с чистой памятью, поэтому
 * счётчики /metrics и скользящее окно rate limit считают в пределах экземпляра,
 * а не всего сервиса. Для полноценных метрик нужен долгоживущий процесс.
 */
import { createApp } from "./app";

export default createApp();
